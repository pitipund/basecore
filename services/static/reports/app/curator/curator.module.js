/**
 * Curator modules declaration
 * @namespace reports.curator
 */
(function () {
    'use strict';

    angular.module('reports.curator', [
        'reports.curator.services'
    ]);

    angular.module('reports.curator.services', []);

})();